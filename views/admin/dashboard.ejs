

<div class=" min-h-screen flex flex-col">

    <%- include('header') %>


        <!-- Main Dashboard Section -->
        <div class="flex flex-grow ">

            <!-- Sidebar -->
            <aside class="bg-gradient-to-r from-gray-200 to-gray-600 text-black  w-64 lg:w-72 fixed top-0 left-0 py-6 px-5 shadow-lg transition-transform transform lg:translate-x-0 -translate-x-full lg:static z-50" 
       id="adminSidebar">
    <!-- Logo -->
    <div class="flex items-center justify-between mb-8">
        <h2 class="text-2xl font-bold tracking-wide">Admin Panel</h2>
        <button class="lg:hidden text-white text-2xl focus:outline-none" onclick="toggleSidebar()">×</button>
    </div>

    <!-- Navigation -->
    <nav class="space-y-4">
        <!-- Dashboard -->
        <a href="/admin/dashboard" class="flex items-center space-x-4 py-3 px-4 rounded-lg bg-white text-[#4A148C] shadow-md hover:bg-gray-200 transition">
            <span class="text-sm font-medium">Dashboard</span>
        </a>

        <!-- Sales report -->
        <a href="/admin/salesReport" class="flex items-center space-x-4 py-3 px-4 rounded-lg hover:bg-white hover:text-[#4A148C] transition">
            <span class="text-sm font-medium">Sales Report</span>
        </a>

        <!-- Products -->
        <a href="/admin/products" class="flex items-center space-x-4 py-3 px-4 rounded-lg hover:bg-white hover:text-[#4A148C] transition">
            <span class="text-sm font-medium">Products</span>
        </a>

        <!-- Category -->
        <a href="/admin/category" class="flex items-center space-x-4 py-3 px-4 rounded-lg hover:bg-white hover:text-[#4A148C] transition">
            <span class="text-sm font-medium">Category</span>
        </a>

        <!-- Customers -->
        <a href="/admin/customers" class="flex items-center space-x-4 py-3 px-4 rounded-lg hover:bg-white hover:text-[#4A148C] transition">
            <span class="text-sm font-medium">Customers</span>
        </a>

        <!-- Orders -->
        <a href="/admin/orders" class="flex items-center space-x-4 py-3 px-4 rounded-lg hover:bg-white hover:text-[#4A148C] transition">
            <span class="text-sm font-medium">Orders</span>
        </a>

        <!-- Coupons -->
        <a href="/admin/coupons" class="flex items-center space-x-4 py-3 px-4 rounded-lg hover:bg-white hover:text-[#4A148C] transition">
            <span class="text-sm font-medium">Coupons</span>
        </a>

        <!-- Offer -->
        <a href="/admin/offer" class="flex items-center space-x-4 py-3 px-4 rounded-lg hover:bg-white hover:text-[#4A148C] transition">
            <span class="text-sm font-medium">Offers</span>
        </a>

        <!-- Banner -->
        <a href="#" class="flex items-center space-x-4 py-3 px-4 rounded-lg hover:bg-white hover:text-[#4A148C] transition">
            <span class="text-sm font-medium">Banner</span>
        </a>

        <!-- Logout -->
        <a href="/admin/logout" class="flex items-center space-x-4 py-3 px-4 rounded-lg hover:bg-white hover:text-[#4A148C] transition">
            <span class="text-sm font-medium">Logout</span>
        </a>
    </nav>
</aside>


            

            <!-- Main Content -->
            <main class="flex-1 p-6 bg-white">
                <!-- Sales Summary -->
                <div class="main-content bg-gray-200 p-6 text-black">
                    <div class="sidebar-header">
                        <h1 class="text-2xl font-bold">Sales Summary</h1>
                    </div>
                    <div class="stats grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mt-6">
                        <div class="stat-item bg-gray-700 p-4 rounded-lg text-center">
                            <p class="text-sm font-medium">Revenue</p>
                            <h2 class="text-2xl font-bold flex items-center justify-center mt-2">
                                <i class="fa-solid fa-indian-rupee-sign mr-2"></i>
                                <%= Revenue %>
                            </h2>
                        </div>
                        <div class="stat-item bg-gray-700 p-4 rounded-lg text-center">
                            <p class="text-sm font-medium">Orders</p>
                            <h2 class="text-2xl font-bold mt-2"><%= orderCount %></h2>
                        </div>
                        <div class="stat-item bg-gray-700 p-4 rounded-lg text-center">
                            <p class="text-sm font-medium">Products Sold</p>
                            <h2 class="text-2xl font-bold mt-2"><%= productCount %></h2>
                        </div>
                        <div class="stat-item bg-gray-700 p-4 rounded-lg text-center">
                            <p class="text-sm font-medium">Total Customers</p>
                            <h2 class="text-2xl font-bold mt-2"><%= userCount %></h2>
                        </div>
                    </div>
                    <div class="container chart-container mt-8">
                        <!-- Nav tabs -->
                        <ul class="flex border-b border-gray-600" id="chartTabs" role="tablist">
                            <li class="mr-4">
                                <button class="nav-link active text-black px-4 py-2 border-b-2 border-transparent hover:border-blue-500 focus:outline-none"
                                    id="orders-tab" data-tab="orders" type="button" role="tab">
                                    Total Sales Count
                                </button>
                            </li>
                            <li class="mr-4">
                                <button class="nav-link text-black px-4 py-2 border-b-2 border-transparent  hover:border-blue-500 focus:outline-none"
                                    id="revenue-tab" data-tab="revenue" type="button" role="tab">
                                    Total Revenue
                                </button>
                            </li>
                            <li>
                                <button class="nav-link text-black px-4 py-2 border-b-2 border-transparent  hover:border-blue-500 focus:outline-none"
                                    id="products-tab" data-tab="products" type="button" role="tab">
                                    Total Products Sold
                                </button>
                            </li>
                        </ul>
                        
                        <div class="tab-content mt-6">
                            <div class="tab-pane active" id="orders" role="tabpanel">
                                <canvas id="ordersChart"></canvas>
                            </div>
                            <div class="tab-pane hidden" id="revenue" role="tabpanel">
                                <canvas id="revenueChart"></canvas>
                            </div>
                            <div class="tab-pane hidden" id="products" role="tabpanel">
                                <canvas id="productsChart"></canvas>
                            </div>
                        </div>
                        
                        
                        
                    </div>
                </div>
                
            </main>
        </div>

        <%- include('bestSelling') %>
        <!-- Footer -->
        <%- include('footer') %>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const tabs = document.querySelectorAll('[data-tab]');
            const panes = document.querySelectorAll('.tab-pane');
    
            tabs.forEach(tab => {
                tab.addEventListener('click', () => {
                    // Remove active class from all tabs and panes
                    tabs.forEach(t => t.classList.remove('border-blue-500', 'active'));
                    panes.forEach(pane => pane.classList.add('hidden'));
    
                    // Add active class to the clicked tab and corresponding pane
                    tab.classList.add('border-blue-500', 'active');
                    document.getElementById(tab.dataset.tab).classList.remove('hidden');
                });
            });
        });

        function createChart(ctx, type, data, label) {
                    return new Chart(ctx, {
                        type: type,
                        data: {
                            labels: ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'],
                            datasets: [{
                                label: label,
                                data: data,
                                backgroundColor: '#D5D2FF',
                                borderColor: '#2b0a5a',
                                borderWidth: 1
                            }]
                        },
                        options: {
                            scales: {
                                y: {
                                    beginAtZero: true
                                }
                            }
                        }
                    });
                }
                // Fetch data from backend and update charts
                async function fetchDataAndRenderCharts() {
                    try {
                        const response = await fetch('/admin/charts');
                        const data = await response.json();

                        // Create charts with fetched data
                        createChart(document.getElementById('ordersChart'), 'line', data.salesData, 'Total Sales Count');
                        createChart(document.getElementById('revenueChart'), 'bar', data.revenueData, 'Total Revenue');
                        createChart(document.getElementById('productsChart'), 'polarArea', data.productsData, 'Total Products Sold');
                    } catch (error) {
                        console.error('Error fetching data:', error);
                    }
                }

                // Initial data fetch and rendering
                fetchDataAndRenderCharts();
    </script>